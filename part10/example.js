/*var num1=parseInt( prompt("Enter the num1:"));
var num2=parseInt( prompt("Enter the num2:"));
var num3=parseInt( prompt("Enter the num3:"));
if(num1>num2 && num1>>num3)
    document.write("larger num is="+num1);
    else if
    (num2>num1 && num2>num3)
    document.write("larger num is="+num2);
    else 
    document.write("larger num is="+num3);*/

    var latter=( prompt("Enter the latter:"));
    var letter=latter.toLowerCase(latter);
    document.write("latter is =" +letter+"<br>");
    if(letter=="a"||letter=="e"||letter=="i"||letter=="o"||letter=="u")
        document.write("vowel");
    else
        document.write("consonant");
    

    


